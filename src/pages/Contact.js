// src/pages/Contact.js
import React from 'react';
import {theme} from '../theme';

const Contact = () => {
    return (
        <div style={{backgroundColor: theme.darkColor, color: '#FFFFFF', minHeight: '100vh'}}>
            <h1>Contact</h1>
            <p>Get in touch with us to learn more about our smart garden.</p>
        </div>
    );
};

export default Contact;
